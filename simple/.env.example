# Django
SECRET_KEY=django-insecure-change-me-in-production-please
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_NAME=re_crm
DB_USER=postgres
DB_PASSWORD=postgre
DB_HOST=localhost
DB_PORT=5432

# DaData — подсказки адресов (сервис https://dadata.ru/)
# Ключ API хранится ТОЛЬКО на сервере и никогда не передаётся в браузер.
# ВАЖНО: URL должен быть полным, до /suggest/address — иначе получите 404.
DADATA_API_URL=https://suggestions.dadata.ru/suggestions/api/4_1/rs/suggest/address
DADATA_API_KEY=8ceded5bba84e0bd3f20cd7a36057324dc680563

# Vite / django-vite
DJANGO_VITE_DEV_MODE=True
DJANGO_VITE_DEV_SERVER_HOST=localhost
DJANGO_VITE_DEV_SERVER_PORT=5173

# SmartPay (SberDevices) — оплата просмотров объектов
# Документация: https://developers.sber.ru/docs/ru/va/about/monetization/payments
# Тестовый токен: max сумма 1 ₽ (100 копеек), реальная карта, возврат за месяц.
SMARTPAY_TOKEN=
SMARTPAY_SERVICE_ID=
SMARTPAY_AMOUNT=100
SMARTPAY_TIMEOUT=15

# SMTP / Email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_TIMEOUT=30
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

# Email templates
AGENCY_NAME=Агентство недвижимости
AGENCY_PUBLIC_URL=http://127.0.0.1:8000
AGENCY_REPLY_TO=
